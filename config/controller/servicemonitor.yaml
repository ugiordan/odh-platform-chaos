apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: odh-chaos-controller
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: odh-chaos-controller
  endpoints:
    - port: metrics
      interval: 30s
