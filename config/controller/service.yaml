apiVersion: v1
kind: Service
metadata:
  name: odh-chaos-controller
  labels:
    app.kubernetes.io/name: odh-chaos-controller
spec:
  selector:
    app.kubernetes.io/name: odh-chaos-controller
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
      protocol: TCP
